package com.implementation.seleniumUtilities;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Actions {
/*
	WebDriver driver=SeleniumDriver.driverIntialize(driver);
	public static void performActionsClick(WebDriver driver,By element)
	{
		Actions action =new Actions(driverIntialize());
		action.movetoElement(driverIntialize().findElement(element)).build().perform();
		SeleniumDriver.driverIntialize().findElement(element).click();
		
	}
	*/
	
}
